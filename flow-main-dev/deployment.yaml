apiVersion: apps/v1
kind: Deployment
metadata:
  name: flow-main-dev
  namespace: flow
  labels:
    app: flow-main-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flow-main-dev
  template:
    metadata:
      labels:
        app: flow-main-dev
    spec:
      containers:
        - name: flow-main-dev
          image: asia-northeast3-docker.pkg.dev/code-review-platform-flow/flow-dev/flow-main-dev:e5ac9459db76965aa4ef544f746b61d190cb4cc4
          ports:
            - containerPort: 8080
          env:
            - name: PROFILE
              value: "dev"
            - name: DB_URL
              valueFrom:
                secretKeyRef:
                  name: flow-main-dev-secret
                  key: DB_URL
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: flow-main-dev-secret
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: flow-main-dev-secret
                  key: DB_PASSWORD
          volumeMounts:
            - name: log-volume
              mountPath: /var/log/flow
      volumes:
        - name: log-volume
          emptyDir: {}

---
apiVersion: v1
kind: Service
metadata:
  name: flow-main-dev-service
  namespace: flow
spec:
  selector:
    app: flow-main-dev
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
