apiVersion: apps/v1
kind: Deployment
metadata:
  name: flow-main-dev
  namespace: flow
  labels:
    app: flow-main-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flow-main-dev
  template:
    metadata:
      labels:
        app: flow-main-dev
    spec:
      containers:
        - name: flow-main-dev
          image: asia-northeast3-docker.pkg.dev/code-review-platform-flow/flow-dev/flow-main-dev:30216e311746bf1c0fd1ac904e9c27effc16efed
          ports:
            - containerPort: 8080
          env:
            - name: PROFILE
              value: "dev"
            - name: DB_URL
              valueFrom:
                secretKeyRef:
                  name: flow-main-dev-secret
                  key: DB_URL
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: flow-main-dev-secret
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: flow-main-dev-secret
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: flow-main-dev-secret
                  key: JWT_SECRET
            - name: ACCESS_EXPIRATION
              valueFrom:
                secretKeyRef:
                  name: flow-main-dev-secret
                  key: ACCESS_EXPIRATION
            - name: REFRESH_EXPIRATION
              valueFrom:
                secretKeyRef:
                  name: flow-main-dev-secret
                  key: REFRESH_EXPIRATION
            - name: PROFILE_URL
              valueFrom:
                secretKeyRef:
                  name: flow-main-dev-secret
                  key: PROFILE_URL
            - name: UNIV_API_KEY
              valueFrom:
                secretKeyRef:
                  name: flow-main-dev-secret
                  key: UNIV_API_KEY
          volumeMounts:
            - name: log-volume
              mountPath: /var/log/flow
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
      tolerations:
        - key: "instance_type"
          operator: "Equal"
          value: "spot"
          effect: "NoSchedule"
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: team
                    operator: In
                    values:
                      - devops
      volumes:
        - name: log-volume
          emptyDir: {}

---
apiVersion: v1
kind: Service
metadata:
  name: flow-main-dev-service
  namespace: flow
spec:
  selector:
    app: flow-main-dev
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
