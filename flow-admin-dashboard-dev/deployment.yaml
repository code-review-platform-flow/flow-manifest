apiVersion: apps/v1
kind: Deployment
metadata:
  name: flow-admin-dashboard-dev
  namespace: flow
  labels:
    app: flow-admin-dashboard-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flow-admin-dashboard-dev
  template:
    metadata:
      labels:
        app: flow-admin-dashboard-dev
    spec:
      containers:
        - name: flow-admin-dashboard-dev
          image: asia-northeast3-docker.pkg.dev/code-review-platform-flow/flow-dev/flow-admin-dashboard-dev:3c377b7d01958efbcd273ef6b40610b99328b06d
          ports:
            - containerPort: 3000
          env:
            - name: APP_ENV
              value: "development"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
      tolerations:
        - key: "instance_type"
          operator: "Equal"
          value: "spot"
          effect: "NoSchedule"
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: team
                    operator: In
                    values:
                      - devops
      volumes:
        - name: log-volume
          emptyDir: {}

---
apiVersion: v1
kind: Service
metadata:
  name: flow-admin-dashboard-dev-service
  namespace: flow
spec:
  selector:
    app: flow-admin-dashboard-dev
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer
